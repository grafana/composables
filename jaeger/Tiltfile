# Jaeger Tilt Extension
# Provides Jaeger all-in-one for distributed tracing.
#
# Exports:
#   cc_export()    - Returns plugin struct for compose_composer
load('ext://compose_composer', 'cc_init')

cc = cc_init('jaeger')

def cc_export():
    """Returns the plugin struct for compose_composer."""
    return cc.create(
        'jaeger',
        os.path.dirname(__file__) + '/jaeger.yaml',
        labels=['observability'],
    )

