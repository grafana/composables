# K3s API Server for Development Testing
# This provides a minimal K3s API server with API aggregation support
# Used by various development environments

services:
  # Certificate cleanup - ensures fresh webhook certs on every startup
  cert-cleanup:
    image: alpine:3.21@sha256:48b0309ca019d89d40f670aa1bc06e426dc0931948452e8491e3d65087abc07d
    volumes:
      - k3s-certs:/certs
    command:
      - sh
      - -c
      - |
        echo "Cleaning webhook TLS certificates to ensure config changes are reflected..."
        rm -rf /certs/tls/
        echo "Webhook certificate cleanup complete"
    restart: "no"

  cert-provider:
    image: alpine:3.21@sha256:48b0309ca019d89d40f670aa1bc06e426dc0931948452e8491e3d65087abc07d
    depends_on:
      cert-cleanup:
        condition: service_completed_successfully
    volumes:
      - k3s-certs:/certs
    environment:
      # Comma-separated list of webhook operators that need certificates
      # Format: service-name:namespace (namespace defaults to 'default' if omitted)
      - WEBHOOK_OPERATORS=${WEBHOOK_OPERATORS:-grafana-servicemodel-operator:default}
    command:
      - sh
      - -c
      - |
        echo "Setting up certificates for K3s API server..."
        mkdir -p /certs/api-server-aggregation /certs/tls
        
        # Generate API server aggregation CA if it doesn't exist
        if [ ! -f /certs/api-server-aggregation/apiserver-client-ca.pem ]; then
          echo "Generating API server aggregation CA certificate..."
          apk add --no-cache openssl
          
          # Generate CA key
          openssl ecparam -genkey -name prime256v1 -out /certs/api-server-aggregation/apiserver-client-ca-key.pem
          
          # Generate CA certificate
          openssl req -new -x509 -key /certs/api-server-aggregation/apiserver-client-ca-key.pem \
            -out /certs/api-server-aggregation/apiserver-client-ca.pem -days 3650 \
            -subj "/CN=Client-CA"
            
          # Generate client key
          openssl ecparam -genkey -name prime256v1 -out /certs/api-server-aggregation/apiserver-client-key.pem
          
          # Generate client certificate signing request
          openssl req -new -key /certs/api-server-aggregation/apiserver-client-key.pem \
            -out /certs/api-server-aggregation/apiserver-client.csr \
            -subj "/CN=front-proxy-client"
            
          # Sign client certificate with CA
          openssl x509 -req -in /certs/api-server-aggregation/apiserver-client.csr \
            -CA /certs/api-server-aggregation/apiserver-client-ca.pem \
            -CAkey /certs/api-server-aggregation/apiserver-client-ca-key.pem \
            -out /certs/api-server-aggregation/apiserver-client.pem \
            -days 365 -CAcreateserial
            
          rm /certs/api-server-aggregation/apiserver-client.csr
        fi
        
        # Generate TLS certificates for webhook operators
        # Always regenerate to reflect current operator configuration
        echo "Generating TLS certificates for webhook operators..."
        apk add --no-cache openssl

        # Parse webhook operators and build Subject Alternative Names
        WEBHOOK_OPERATORS=$${WEBHOOK_OPERATORS:-grafana-servicemodel-operator:default}
        echo "Webhook operators: $$WEBHOOK_OPERATORS"

        # Start building SAN list
        SAN_LIST=""

        # Process each operator
        OPERATORS=$$(echo "$$WEBHOOK_OPERATORS" | tr ',' ' ')
        for operator in $$OPERATORS; do
          # Split operator:namespace (default to 'default' namespace)
          if [[ "$$operator" == *:* ]]; then
            OP_NAME="$${operator%:*}"
            OP_NAMESPACE="$${operator#*:}"
          else
            OP_NAME="$$operator"
            OP_NAMESPACE="default"
          fi

          echo "Processing operator: $$OP_NAME in namespace: $$OP_NAMESPACE"

          # Add DNS names for this operator
          if [ -n "$$SAN_LIST" ]; then
            SAN_LIST="$$SAN_LIST,"
          fi
          SAN_LIST="$${SAN_LIST}DNS:$$OP_NAME"
          SAN_LIST="$$SAN_LIST,DNS:$$OP_NAME.$$OP_NAMESPACE"
          SAN_LIST="$$SAN_LIST,DNS:$$OP_NAME.$$OP_NAMESPACE.svc"
          SAN_LIST="$$SAN_LIST,DNS:$$OP_NAME.$$OP_NAMESPACE.svc.cluster.local"
        done

        # Add common SAN entries
        SAN_LIST="$$SAN_LIST,DNS:localhost,IP:127.0.0.1"

        echo "Generated SAN list: $$SAN_LIST"

        # Use first operator as the primary CN
        FIRST_OPERATOR=$$(echo "$$OPERATORS" | cut -d' ' -f1)
        if [[ "$$FIRST_OPERATOR" == *:* ]]; then
          PRIMARY_CN="$${FIRST_OPERATOR%:*}"
        else
          PRIMARY_CN="$$FIRST_OPERATOR"
        fi

        openssl req -x509 -newkey rsa:4096 -keyout /certs/tls/tls.key -out /certs/tls/tls.crt \
          -days 365 -nodes -subj "/CN=$$PRIMARY_CN" \
          -addext "subjectAltName=$$SAN_LIST"
        
        echo "Certificate setup complete. Listing contents of /certs/api-server-aggregation/ and /certs/tls/"
        ls -la /certs/api-server-aggregation/
        ls -la /certs/tls/
    restart: "no"

  traefik:
    image: traefik:v3.0
    container_name: traefik-webhook-proxy
    ports:
      - '${TRAEFIK_PORT:-9443}:443'   # HTTPS for webhook traffic (unprivileged host port)
      - '${TRAEFIK_DASHBOARD_PORT:-8080}:8080' # Traefik dashboard
    volumes:
      - k3s-certs:/certs:ro # Access to webhook certificates
      - k3s-certs:/etc/traefik/certs:ro # Mount certificates for Traefik TLS
    environment:
      # Same webhook operators list for network aliases
      - WEBHOOK_OPERATORS=${WEBHOOK_OPERATORS:-grafana-servicemodel-operator:default}
    depends_on:
      cert-provider:
        condition: service_completed_successfully
    entrypoint: ["/bin/sh"]
    command:
      - "-c"
      - |
        # Wait for TLS certificates to be ready (prevent race condition with cert-provider)
        echo "Waiting for TLS certificates to be available and valid..."
        while [ ! -f /etc/traefik/certs/tls/tls.crt ] || [ ! -s /etc/traefik/certs/tls/tls.crt ]; do
          echo "Waiting for tls.crt to be created and non-empty..."
          sleep 1
        done
        while [ ! -f /etc/traefik/certs/tls/tls.key ] || [ ! -s /etc/traefik/certs/tls/tls.key ]; do
          echo "Waiting for tls.key to be created and non-empty..."
          sleep 1
        done
        echo "TLS certificates are ready"

        # Create dynamic TLS configuration using the existing webhook certificates
        mkdir -p /etc/traefik/dynamic
        cat > /etc/traefik/dynamic/tls.yml << 'EOF'
        tls:
          certificates:
            - certFile: /etc/traefik/certs/tls/tls.crt
              keyFile: /etc/traefik/certs/tls/tls.key
              stores:
                - default
          stores:
            default:
              defaultCertificate:
                certFile: /etc/traefik/certs/tls/tls.crt
                keyFile: /etc/traefik/certs/tls/tls.key
        EOF

        # Create dynamic routing configuration for webhook operators
        # This replaces Docker label-based routing since we can't access the Podman socket
        WEBHOOK_OPERATORS=$${WEBHOOK_OPERATORS:-grafana-servicemodel-operator:default}
        echo "Configuring routes for webhook operators: $$WEBHOOK_OPERATORS"

        OPERATORS=$$(echo "$$WEBHOOK_OPERATORS" | tr ',' ' ')
        for operator in $$OPERATORS; do
          # Split operator:namespace (default to 'default' namespace)
          if [[ "$$operator" == *:* ]]; then
            OP_NAME="$${operator%:*}"
            OP_NAMESPACE="$${operator#*:}"
          else
            OP_NAME="$$operator"
            OP_NAMESPACE="default"
          fi

          echo "Creating route for operator: $$OP_NAME"

          # Create routing configuration for this operator
          cat > /etc/traefik/dynamic/$$OP_NAME.yml << EOF
        http:
          routers:
            $${OP_NAME}-router:
              rule: "Host(\`$$OP_NAME.$$OP_NAMESPACE.svc.cluster.local\`)"
              service: $${OP_NAME}-service
              entryPoints:
                - websecure
              tls: {}

            $${OP_NAME}-mutate-router:
              rule: "Host(\`$$OP_NAME.$$OP_NAMESPACE.svc.cluster.local\`) && Path(\`/mutate\`)"
              service: $${OP_NAME}-service
              entryPoints:
                - websecure
              tls: {}
              priority: 100

            $${OP_NAME}-validate-router:
              rule: "Host(\`$$OP_NAME.$$OP_NAMESPACE.svc.cluster.local\`) && Path(\`/validate\`)"
              service: $${OP_NAME}-service
              entryPoints:
                - websecure
              tls: {}
              priority: 100

          services:
            $${OP_NAME}-service:
              loadBalancer:
                servers:
                  - url: "https://$$OP_NAME:8443"
                serversTransport: insecureTransport

          serversTransports:
            insecureTransport:
              insecureSkipVerify: true
        EOF

          echo "Created routing configuration for $$OP_NAME"
        done

        echo "Starting Traefik with webhook proxy configuration..."
        exec traefik \
          --log.level=INFO \
          --api.dashboard=true \
          --api.insecure=true \
          --entrypoints.websecure.address=:443 \
          --serverstransport.insecureskipverify=true \
          --providers.file.directory=/etc/traefik/dynamic \
          --providers.file.watch=true
    labels:
      - traefik.enable=false  # Don't proxy Traefik itself
    networks:
      default:
        aliases: 
          - grafana-servicemodel-operator.default.svc.cluster.local

  k3s-apiserver:
    image: rancher/k3s:v1.33.12-k3s1@sha256:173eb786fa098114e49b4e3f9a2a711602451ef884961b0a821015f068e08209
    hostname: k3s-apiserver
    privileged: true
    depends_on:
      cert-provider:
        condition: service_completed_successfully
    command:
      - server
      - --disable-agent
      - --disable-scheduler
      - --disable-controller-manager
      - --disable=coredns
      - --disable=servicelb
      - --disable=traefik
      - --disable=local-storage
      - --disable=metrics-server
      - --disable-network-policy
      - --disable-helm-controller
      - --disable-cloud-controller
      - --tls-san=127.0.0.1
      - --tls-san=k3s-apiserver
      - --write-kubeconfig-mode=644
      - --write-kubeconfig=/output/kubeconfig.yaml
      # API aggregation support
      - --kube-apiserver-arg=requestheader-client-ca-file=/etc/kubernetes/pki/api-server-aggregation/apiserver-client-ca.pem
      - --kube-apiserver-arg=requestheader-allowed-names=front-proxy-client,My Client
      - --kube-apiserver-arg=feature-gates=KubeletInUserNamespace=true
      # Disable built-in traefik to avoid conflicts with our custom Traefik
      - --disable=traefik
    ports:
      - '${K3S_API_SERVER_PORT:-6443}:6443'  # Kubernetes API server port
    volumes:
      - k3s-data:/var/lib/rancher/k3s # Persistence for K3s data (including etcd)
      - k3s-certs:/etc/kubernetes/pki:ro # Mount certificates from shared volume
      - k3s-output:/output # Shared volume for kubeconfig (accessible by other containers)

  # Sync kubeconfig to local filesystem for local kubectl access
  kubeconfig-sync:
    image: alpine:3.21@sha256:48b0309ca019d89d40f670aa1bc06e426dc0931948452e8491e3d65087abc07d
    depends_on:
      - k3s-apiserver
    volumes:
      - k3s-output:/output:ro
      - ./output:/output-local
    entrypoint: ["/bin/sh"]
    command:
      - "-c"
      - |
        echo "Waiting for kubeconfig to be generated..."
        while [ ! -f /output/kubeconfig.yaml ]; do
          sleep 1
        done
        echo "Copying kubeconfig to local filesystem..."
        cp /output/kubeconfig.yaml /output-local/kubeconfig.yaml
        echo "Kubeconfig synced to ./output/kubeconfig.yaml"
    restart: "no"

  # Generic CRD loader - loads Custom Resource Definitions into k3s
  # CRD paths are mounted at /crds/* by orchestrators via compose_composer 'crds' key
  crd-loader:
    image: bitnami/kubectl:latest@sha256:e2b97dde9666986c61c56d49aae85a714b89b69392baa531438e74ec34096fb4
    depends_on:
      - k3s-apiserver
    volumes:
      - k3s-output:/output:ro
      # Additional /crds/* mounts are added dynamically by compose_composer
      # when orchestrators specify 'crds' in their dependency definitions
    entrypoint: ["/bin/sh"]
    command:
      - "-c"
      - |
        echo 'CRD Loader starting...'
        
        # Check if any CRDs are mounted
        if [ ! -d /crds ] || [ -z "$$(ls -A /crds 2>/dev/null)" ]; then
          echo 'No CRDs mounted at /crds/, nothing to load'
          exit 0
        fi
        
        echo 'Waiting for kubeconfig to be available...'
        while [ ! -f /output/kubeconfig.yaml ]; do
          echo 'Kubeconfig not yet available, waiting...'
          sleep 2
        done
        
        echo 'Creating modified kubeconfig for container networking...'
        sed 's/127\.0\.0\.1:6443/k3s-apiserver:6443/g' /output/kubeconfig.yaml > /tmp/kubeconfig-internal.yaml
        
        echo 'Waiting for K3s API server to be ready...'
        until kubectl --kubeconfig=/tmp/kubeconfig-internal.yaml get nodes; do
          echo 'K3s API server not ready, waiting...'
          sleep 5
        done
        
        echo 'K3s API server is ready, loading CRDs...'
        
        # Find and apply all CRD files (JSON and YAML)
        find /crds -type f \( -name "*.json" -o -name "*.yaml" -o -name "*.yml" \) | while read crd; do
          # Sanity check: verify file contains a CustomResourceDefinition
          if grep -q "kind:[[:space:]]*CustomResourceDefinition" "$$crd" 2>/dev/null || \
             grep -q '"kind"[[:space:]]*:[[:space:]]*"CustomResourceDefinition"' "$$crd" 2>/dev/null; then
            echo "Loading CRD: $$crd"
            kubectl --kubeconfig=/tmp/kubeconfig-internal.yaml apply -f "$$crd" || true
          else
            echo "Skipping (not a CRD): $$crd"
          fi
        done
        
        echo 'CRD loading complete'
    restart: "no"

  # Generic admission webhook setup service - use register_admission_config() to customize
  admission-setup:
    image: bitnami/kubectl:latest@sha256:e2b97dde9666986c61c56d49aae85a714b89b69392baa531438e74ec34096fb4
    depends_on:
      - crd-loader  # Ensures CRDs are loaded first
    volumes:
      - k3s-output:/output:ro
      - k3s-certs:/certs:ro
      # admission-config volume mounted by register_admission_config() helper
    entrypoint: ["/bin/sh"]
    command:
      - "-c"
      - |
        echo 'Admission setup starting...'

        echo 'Waiting for kubeconfig to be available...'
        while [ ! -f /output/kubeconfig.yaml ]; do
          echo 'Kubeconfig not yet available, waiting...'
          sleep 2
        done
        echo 'Creating modified kubeconfig for container networking...'
        sed 's/127\.0\.0\.1:6443/k3s-apiserver:6443/g' /output/kubeconfig.yaml > /tmp/kubeconfig-internal.yaml

        echo 'Waiting for TLS certificate to be available...'
        while [ ! -f /certs/tls/tls.crt ]; do
          echo 'TLS certificate not yet available, waiting...'
          sleep 2
        done

        echo 'Generating caBundle from current TLS certificate...'
        CA_BUNDLE=$$(cat /certs/tls/tls.crt | base64 | tr -d '\n')
        echo "CA Bundle generated (length: $$(echo $$CA_BUNDLE | wc -c) chars)"

        echo 'Patching admission config with current caBundle...'
        sed "s|caBundle: .*|caBundle: $$CA_BUNDLE|g" /admission/config.yaml > /tmp/admission-patched.yaml

        echo 'Loading admission webhook configuration...'
        kubectl --kubeconfig=/tmp/kubeconfig-internal.yaml apply -f /tmp/admission-patched.yaml
        echo 'Admission webhook configuration loaded successfully'
    restart: "no"

volumes:
  k3s-data:
  k3s-certs:
  k3s-output: