# Minimal docker-compose for grafana integration tests
# This file is intentionally minimal - dependencies provide the real services

services:
  # Dummy service to satisfy compose_composer
  # Real services come from grafana, k3s-apiserver, and mysql composables
  test-complete:
    image: alpine:latest
    command: echo "Grafana integration tests - check .cc/ for results"
    restart: "no"
